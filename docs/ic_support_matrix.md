# COSEM IC Support Matrix

## Scope

This matrix tracks implemented COSEM interface class coverage against
DLMS/COSEM, СПОДЭС and СПОДУС needs. It is intentionally stricter than the
generic object extension point: a class is `Supported` only when a built-in
object exposes the class-specific attributes or methods and has deterministic
tests.

Status values:

- `Supported`: built-in implementation and tests exist.
- `Partial`: generic extension point or minimal object exists, but class
  semantics are incomplete.
- `Planned`: required for production coverage, not implemented yet.
- `Application-provided`: the framework can host it through `ICosemObject`, but
  there is no built-in implementation.

## Core Model

| Area | Status | Notes |
| --- | --- | --- |
| `ICosemObject` extension point | Supported | Users can implement custom IC behavior. |
| `ObjectRegistry` | Supported | Object lookup by class id and logical name exists. |
| `LogicalDevice` | Supported | Attribute/method dispatch and association view construction exist. |
| Access rights checks | Partial | Basic attribute/method modes exist; association/security-context-specific policy needs expansion. |
| Association object list | Partial | Built from registered objects; full access-rights modeling per association is incomplete. |
| OBIS catalog | Planned | No СПОДЭС/СПОДУС catalog or validation layer exists. |

## Interface Classes

| IC | Name | Status | Notes |
| --- | --- | --- | --- |
| `1` | Data | Supported | `CosemDataObject` exposes logical name and value, class version `0`, and a version-taking constructor normalized to `MaxSupportedVersion`; built-in helpers cover logical device name and public invocation counter objects. |
| `3` | Register | Supported | `CosemRegisterObject` exposes `value` (opaque AXDR data item, type per spec §4.3.2 instance) and typed `scaler_unit` (`types::ScalerUnit` — `scal_unit_type ::= structure { integer scaler, enum unit }` per IEC 62056-6-2 ED4 (2021) §4.3.2.2.3 and DLMS UA Blue Book Ed. 12.1), class version `0`, and a version-taking constructor normalized to `MaxSupportedVersion`. logical_name (`1`) and scaler_unit (`3`) are read-only; value (`2`) access mode is caller-selected. Constructor is safe-fallback: an empty `value` buffer is dropped rather than retained, leaving the attribute cleared until the backend publishes a real value via `SetValue` (which also rejects empty buffers). `WriteAttribute(value)` rejects empty payloads with `InvalidArgument`; static `IsValidValue` exposes the same check pre-construction. Method `1` `reset` (data ::= integer(0)) is surfaced as `UnsupportedFeature`; application-defined semantics decide what reset means for a given register instance and the built-in object does not own that policy. Other method ids report `MethodNotFound`. |
| `4` | Extended Register | Partial | `CosemExtendedRegisterObject` exposes `value` (opaque AXDR data item, per-instance CHOICE), typed `scaler_unit` (`types::ScalerUnit` — `scal_unit_type ::= structure { integer scaler, enum unit }` per IEC 62056-6-2 ED4 (2021) §4.3.3 and DLMS UA Blue Book Ed. 12.1), opaque `status` (per-instance CHOICE, kept raw until a discriminated-union migration covers IC 4/5 jointly) and typed `capture_time` (`types::DateTime` — 12-byte `octet-string(date_time)`). Class version `0`, version-taking constructor normalized to `MaxSupportedVersion`. logical_name (`1`), scaler_unit (`3`), status (`4`) and capture_time (`5`) are read-only; value (`2`) access mode is caller-selected. Constructor is safe-fallback: an empty `value` buffer is dropped rather than retained, leaving the attribute cleared until the backend publishes a real value via `SetValue` (which rejects empty buffers and returns `bool`). `WriteAttribute(value)` rejects empty payloads with `InvalidArgument`; static `IsValidValue` exposes the same check pre-construction. Method `1` `reset` is surfaced as `UnsupportedFeature` (application-defined semantics); other method ids report `MethodNotFound`. Listed Partial because `value` and `status` remain opaque pending a typed discriminated-union representation. |
| `5` | Demand Register | Partial | `CosemDemandRegisterObject` exposes `current_average_value` / `last_average_value` (opaque AXDR data items, per-instance CHOICE), typed `scaler_unit` (`types::ScalerUnit` — `scal_unit_type ::= structure { integer scaler, enum unit }` per IEC 62056-6-2 ED4 (2021) §4.3.4 and DLMS UA Blue Book Ed. 12.1), opaque `status` (per-instance CHOICE, kept raw until a discriminated-union migration covers IC 4/5 jointly), typed `capture_time` and `start_time_current` (both `types::DateTime` — 12-byte `octet-string(date_time)`), and integral `period` (`double-long-unsigned`) / `number_of_periods` (`long-unsigned`). Class version `0`, version-taking constructor normalized to `MaxSupportedVersion`. All attributes are read-only. Constructor is safe-fallback: empty `current_average_value` / `last_average_value` buffers are dropped rather than retained, leaving the attributes cleared until the backend publishes real values via `SetCurrentAverageValue` / `SetLastAverageValue` (which both return `bool` and reject empty buffers). Static `IsValidAverageValue` exposes the same pre-construction check. Methods `1` `reset` and `2` `next_period` are surfaced as `UnsupportedFeature` (application-defined semantics); other method ids report `MethodNotFound`. Listed Partial because `current_average_value`, `last_average_value` and `status` remain opaque AXDR pending a typed discriminated-union representation. |
| `6` | Register Activation | Yes | `CosemRegisterActivationObject` exposes typed `register_assignment` (`std::vector<types::ObjectDefinition>` — each entry is `structure { long-unsigned class_id, octet-string(6) logical_name }`), typed `mask_list` (`std::vector<types::RegisterMask>` — each entry is `structure { octet-string mask_name, array of long-unsigned index_list }`) and `active_mask` (octet-string), all encoded on the fly per IEC 62056-6-2 ED4 (2021) §4.3.5 and DLMS UA Blue Book Ed. 12.1 §4.3.5. Class version `0`, version-taking constructor normalized to `MaxSupportedVersion`. All attributes are read-only; mutation is owned by the caller via typed setters (`SetRegisterAssignment` / `SetMaskList` / `SetActiveMask`). Methods `1` `add_register`, `2` `add_mask` and `3` `delete_mask` return explicit `UnsupportedFeature` (application-defined semantics owned by the backend's object catalogue); other method ids report `MethodNotFound`. |
| `7` | Profile Generic | Partial | `CosemProfileGenericObject` defaults to class version `1`, allows explicit descriptor version selection up to `MaxSupportedVersion`, and exposes read-only profile attributes including configurable `sort_method` / `sort_object` plus class-level helpers for `buffer`, `capture_objects`, range descriptor and entry descriptor composite encoding/decoding. Method `1` `reset` is implemented and clears the stored profile buffer (`entries_in_use` drops to `0` on the next read); methods `2` `capture`, `3` `get_buffer_by_range` and `4` `get_buffer_by_index` remain `UnsupportedFeature` because they require backend-driven "now" semantics and selective-access wiring. Capture-object / range-descriptor / entry-descriptor AXDR codecs are exposed as free-standing helpers so backends can implement selective access on top of the existing object. Full typed-value migration of `capture_objects` / `buffer` and selective-access integration into `ReadAttribute` are planned. |
| `8` | Clock | Supported | `CosemClockObject` exposes class version `0` documented clock attributes via the typed `types::DateTime` value (validated wildcards, range-checked fields) with read/write support where implemented; backends call `SetTime`/`SetStatus` to publish authoritative state. Adjust/preset/shift methods are explicit unsupported features. |
| `9` | Script Table | Yes | `CosemScriptTableObject` models `scripts` as `std::vector<types::ScriptEntry>` with typed `types::ActionSpecification` (enum `service_id` + `class_id` + `logical_name` + `index` + opaque `parameter` raw-AXDR). Class version `0`, version-taking constructor normalized to `MaxSupportedVersion`. logical_name (`1`) is read-only; `scripts` (`2`) access mode caller-selected. Constructors / `SetScripts` enforce per-action `IsValid` + unique `script_identifier` (safe-fallback to empty on violation; setter returns `bool`). `WriteAttribute` decodes the IEC 62056-6-2 ED4 §4.5.2 wire form (`array of script`, `script ::= structure(2)`, `action_specification ::= structure(5)`), captures `parameter` as raw AXDR via `SkipDlmsData`, validates per-action invariants + uniqueness + no-trailing-bytes, returning `InvalidArgument` without mutation on any failure. `ReadAttribute` re-encodes from the typed model; empty `parameter` round-trips as AXDR `null-data`. Method `1` `execute` is surfaced as `UnsupportedFeature` (application-defined script semantics); other method ids report `MethodNotFound`. |
| `10` | Schedule | Yes | `CosemScheduleObject` models `entries` as a typed `std::vector<dlms::cosem::types::ScheduleTableEntry>` (`{ index: long-unsigned, enable: boolean, script: { logical_name: octet-string(6), selector: long-unsigned }, switch_time: octet-string(4)=time, validity_window: long-unsigned (0xFFFF=always), exec_weekdays: bit-string(7) (bit 0 = Mon … bit 6 = Sun), exec_specdays: bit-string(64) (bit i = day_id i, capped to 0..63 by spec choice A), begin_date: octet-string(5)=date, end_date: octet-string(5)=date }`). Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; entries (`2`) honor a caller-selected `AttributeAccessMode`. `ReadAttribute` encodes the typed collection on demand; `WriteAttribute` decodes the wire form, validates every field and the collection invariant (unique `index`), then swaps atomically — malformed AXDR, wrong tags/lengths/bit-string widths, invalid times/dates and duplicate indices all return `CosemStatus::InvalidArgument` without touching the existing entries. Safe-fallback constructor: passing an invalid collection yields an empty `entries_` rather than holding an inconsistent state. Methods are fully implemented per IEC 62056-6-2 ED4 §4.5.3.3: `1` `enable_disable(structure { first_disable, last_disable, first_enable, last_enable })` disables range A first, then enables range B (so when ranges overlap, the enabling side wins per spec); ranges with `first>last` or `first>9999` are no-ops; `last>9999` is capped to 9999. `2` `insert(schedule_table_entry)` appends a new entry or overwrites the existing one sharing the new `index`. `3` `delete(structure { first_index, last_index })` removes all entries whose `index` falls in the inclusive range; `first>last` is a no-op. All three return `Ok` on success and `InvalidArgument` on malformed AXDR; other method ids report `MethodNotFound`. |
| `11` | Special Days Table | Supported | `CosemSpecialDaysTableObject` exposes `entries` as a typed `std::vector<types::SpecialDayEntry>` (`{ index: long-unsigned, specialday_date: types::Date, day_id: unsigned }`). Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; entries (`2`) honor a caller-selected `AttributeAccessMode`. `WriteAttribute` decodes the AXDR `array of structure(3) { long-unsigned, octet-string(5), unsigned }` payload, validates each `Date` field-by-field, and enforces the spec uniqueness invariant (unique `index` and unique `specialday_date` across the collection) before swapping; malformed payloads and invariant violations return `InvalidArgument` without mutating state. `SetEntries()` performs the same validation for backend-driven refresh and returns `bool`. Methods `1` `insert(spec_day_entry)` and `2` `delete(long-unsigned index)` are fully implemented per §4.5.4.3.1: `insert` overwrites any entry sharing the new index *or* the new date (so the invariant still holds even when both keys collide with different existing entries); `delete` is a no-op for missing indices and still returns `Ok`. Other method ids report `MethodNotFound`. |
| `12` | Association SN | Partial | `CosemAssociationSnObject` exposes object_list (array of structure {`base_name`: long-int, `class_id`: long-unsigned, `version`: unsigned, `logical_name`: octet-string(6), `access_rights`: structure}), access_rights_list (array of structure), security_setup_reference (octet-string(6) LN to Security Setup, v2+), user_list (array of structure {`user_id`: unsigned, `user_name`: visible-string}, v3+) and current_user (structure {`user_id`: unsigned, `user_name`: visible-string}, v3+) as opaque encoded DLMS Data buffers prepared by the caller. Class version up to `4`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`3` always, `4` when v>=2 and `5`-`6` when v>=3 share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed object list, access rights list, security setup reference and user lists after authentication, secret rotation or list mutations performed out-of-band). Lower-version instances clear gated buffers, report `NoAccess` for them and return `AttributeNotFound` on reads/writes. Specific methods `3` `read_by_logicalname`, `5` `change_secret` and `8` `reply_to_HLS_authentication` return `UnsupportedFeature` on every version; `9` `add_user` and `10` `remove_user` return `UnsupportedFeature` for v>=3 and `MethodNotFound` otherwise. Reserved method ids `1`, `2`, `4`, `6`, `7` and `11+` return `MethodNotFound`. |
| `15` | Association LN | Partial | Built-in object supports caller-selected versions up to `3` with version-gated object list, association status, optional security setup reference (v1+), user-list/current-user attributes (v3+), explicit unsupported methods `1`-`4` (v0+) and `5`-`6` (v3+), and class-level encode/decode helpers for `object_list` and access-right structures. Full HLS method execution and association-specific policy rebinding are incomplete. |
| `17` | SAP Assignment | Supported | `CosemSapAssignmentObject` exposes class version `0` and a version-taking constructor normalized to `MaxSupportedVersion`. Method `1` `connect_logical_device` dispatches application-defined SAP/LD attachment policy and is surfaced as `UnsupportedFeature`; other method ids report `MethodNotFound`. |
| `18` | Image Transfer | Partial | `CosemImageTransferObject` exposes image_block_size, image_transferred_blocks_status, image_first_not_transferred_block_number, image_transfer_enabled, image_transfer_status and image_to_activate_info attributes as opaque encoded DLMS Data buffers prepared by the caller, class version `0`, and a version-taking constructor normalized to `MaxSupportedVersion`. `image_transfer_enabled` (attr 5) access mode is caller-selected; logical_name and all other attributes are read-only. Methods `1` `image_transfer_initiate`, `2` `image_block_transfer`, `3` `image_verify` and `4` `image_activate` are surfaced as `UnsupportedFeature` (application-defined firmware transfer/storage); other method ids report `MethodNotFound`. |
| `19` | IEC Local Port Setup | Partial | `CosemIecLocalPortSetupObject` exposes default_mode (enum), default_baud (enum), proposed_baud (enum), response_time (enum), device_address (octet-string with the device-address logical name) and password_1 / password_2 / password_5 (octet-strings carrying the level-1, level-2 and level-5 passwords) as opaque encoded DLMS Data buffers prepared by the caller. Class version `1`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`9` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed mode, baud, response time, device address and passwords after configuration changes out-of-band). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `20` | Activity Calendar | Yes | `CosemActivityCalendarObject` models season_profile / week_profile_table / day_profile_table (active and passive) as typed `std::vector<types::SeasonProfile>` / `std::vector<types::WeekProfile>` / `std::vector<types::DayProfile>` per IEC 62056-6-2 ED4 (2021) §4.5.5 / Blue Book Ed. 12.1 §5.1.9. `activate_passive_calendar_time` is a typed `types::DateTime` with wildcards allowed (an all-wildcard value means "never activates" per the spec note). `calendar_name_active`/`calendar_name_passive` remain opaque octet-strings. Class version `0` with a version-taking constructor normalized to `MaxSupportedVersion`. logical_name (`1`) and active attributes (`2`-`5`) are read-only; passive attributes (`6`-`9`) and `activate_passive_calendar_time` (`10`) share a caller-selected access mode. Constructors and `SetSeasonProfilePassive` / `SetWeekProfileTablePassive` / `SetDayProfileTablePassive` enforce intra-collection invariants (unique season names with strictly ascending start date_time, unique week names, unique day_ids, ascending day_schedule start_time) and cross-collection invariants (every week day_id resolves in day_profile_table, every season week_name resolves in week_profile_table). Setters return `bool` and do not mutate on failure; constructors drop an inconsistent collection rather than holding invalid state. `WriteAttribute` decodes the wire form, validates it, and returns `CosemStatus::InvalidArgument` on malformed AXDR / unknown tags / out-of-range fields / broken cross-references. Method `1` `activate_passive_calendar` is surfaced as `UnsupportedFeature` (application-defined activation policy); other method ids report `MethodNotFound`. |
| `21` | Register Monitor | Yes | `CosemRegisterMonitorObject` exposes `thresholds` (typed `std::vector<CosemByteBuffer>` — one opaque-per-entry, the per-element AXDR type matches the monitored attribute), `monitored_value` (typed `types::MonitoredValue` = `value_definition` structure `{class_id, logical_name, attribute_index}`) and `actions` (typed `std::vector<types::ActionSet>`, where `action_set ::= {action_up, action_down}` and each `action_item` is `types::Script {script_logical_name, script_selector}`) per IEC 62056-6-2 ED4 (2021) §4.5.6. Class version `0`; constructor normalizes higher versions to `MaxSupportedVersion`. `thresholds` access mode is caller-selected; `monitored_value` and `actions` are read-only. Constructor is safe-fallback: `thresholds` and `actions` are dropped together when sizes mismatch or any threshold entry is empty AXDR, leaving the object with a consistent empty pair rather than invalid state. `WriteAttribute(thresholds)` decodes the array of opaque per-entry AXDR (`SkipDlmsData`), rejects trailing bytes, empty entries, and size mismatches against current `actions` with `InvalidArgument`; swaps only on success. `SetThresholds`/`SetActions` enforce the same `|thresholds| == |actions|` invariant and refuse mutation otherwise; `SetMonitoredValue` rejects invalid values (`attribute_index < 1`). Static validators `IsValidThresholds` and `ThresholdsMatchActions` expose the invariants pre-construction. Register Monitor v0 defines no methods, so every method id returns `MethodNotFound`. |
| `22` | Single Action Schedule | Yes | `CosemSingleActionScheduleObject` models executed_script via `types::Script` (`{logical_name, selector}`), type via `types::SingleActionScheduleType` (enum 1..5 with spec invariant predicates) and execution_time as `vector<pair<types::Time, types::Date>>`. Wire encoding matches Blue Book Ed. 12.1 §4.5.7. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode`. Cross-field invariants from §4.5.7.2.3 are enforced (type=1 ⇒ single entry; type∈{2,3} ⇒ uniform time; type∈{2,4} ⇒ no wildcard in date; hundredths_of_second must be 0 or unspecified): constructors safely fall back to a 1-entry wildcard schedule on violation, `SetType` / `SetExecutionTime` return `false` without mutating, and `WriteAttribute` returns `InvalidArgument` for malformed AXDR, out-of-range enum, empty array, or invariant violation. IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `23` | IEC HDLC Setup | Supported | `CosemIecHdlcSetupObject` exposes all seven dynamic attributes as typed values per IEC 62056-6-2 ED4 (2021) §4.7.2.2.2 and DLMS UA Blue Book Ed. 12.1: `2 comm_speed` as nested enum `CommSpeed` (`Baud300=0`..`Baud115200=9`), `3 window_size_transmit` and `4 window_size_receive` as `std::uint8_t` clamped to `[1,7]`, `5 max_info_field_length_transmit` and `6 max_info_field_length_receive` as `std::uint16_t` clamped to `[32,2030]`, `7 inter_octet_time_out` as `std::uint16_t` ms clamped to `[20,6000]`, `8 inactivity_time_out` as `std::uint16_t` seconds (`0` disables the timer), and `9 device_address` as `std::uint16_t` clamped to `[0x0010,0x3FFD]` (read-only at the wire, refreshed server-side via `SetDeviceAddress`, which returns `bool` and rejects out-of-range values). Class version `1`, constructor normalization to `MaxSupportedVersion`; all ctor inputs are clamped/normalised so the object never holds invalid state. `ReadAttribute(2)` encodes AXDR `enum`; `(3)`/`(4)` encode AXDR `unsigned`; `(5)`-`(9)` encode AXDR `long-unsigned`. `WriteAttribute(2)`-`(8)` decode the matching wire forms, enforce the Blue Book ranges, and return `InvalidArgument` on malformed input or out-of-range values (stored value preserved on rejection); `WriteAttribute(1)` and `WriteAttribute(9)` always return `AccessDenied`. Five static validators are exported: `IsValidCommSpeed`, `IsValidWindowSize`, `IsValidMaxInfoFieldLength`, `IsValidInterOctetTimeOut`, `IsValidDeviceAddress`. The getter previously named `CommSpeed()` was renamed to `GetCommSpeed()` so the unqualified name denotes the nested enum (same pattern as `CosemDisconnectControlObject::ControlMode`). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `24` | IEC twisted pair setup | Partial | `CosemIecTwistedPairSetupObject` exposes the five spec attributes per IEC 62056-6-2 ED4 (2021) §4.7.3 and DLMS UA Blue Book Ed. 12.1 §4.7.3: `2 secondary_address` (octet-string of size 6), `3 primary_address_list` (array of octet-string), `4 tabi_list` (array of integer) and `5 fatal_error` (enum) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted); `5 fatal_error` is server-managed and remains read-only at the wire surface regardless of the caller-selected mode. IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `25` | M-Bus slave port setup | Partial | `CosemMBusSlavePortSetupObject` exposes default_baud (enum), avail_baud (enum), addr_state (enum indicating whether the slave has been assigned a bus address) and bus_address (unsigned) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`5` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IEC 62056-6-2 ED4 (2021) §4.8.2 and DLMS UA Blue Book Ed. 12.1 §4.8.1 leave the "Specific methods | m/o" column empty for this IC; `InvokeMethod` therefore returns `MethodNotFound` for every id. Earlier revisions exposed a phantom `reset` method (id `1`) and used non-spec attribute names `status`/`mbus_port_reference` for attributes `4` and `5`; the constructor and accessor signature have been rebuilt to align with the spec. |
| `26` | Utility Tables | Partial | `CosemUtilityTablesObject` exposes table_id (long-unsigned), length (double-long-unsigned) and buffer (octet-string containing the raw table payload) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `27` | Modem Configuration | Partial | `CosemModemConfigurationObject` exposes communication_speed (enum), initialisation_strings (array of structure) and modem_profile (array of octet-string) as opaque encoded DLMS Data buffers prepared by the caller. Class version `1`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `28` | Auto Answer | Partial | `CosemAutoAnswerObject` exposes mode (enum), listening_window (array of structure), status (enum, RO with setter for backend-driven refresh), number_of_calls (unsigned), number_of_rings (structure of in/out-of-window ring counts) and list_of_allowed_callers (array of `allowed_caller_element`) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) and status (`4`) are read-only; attributes `2`, `3`, `5`, `6` and `7` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `29` | Auto Connect | Partial | `CosemAutoConnectObject` exposes mode (enum), repetitions (unsigned), repetition_delay (long-unsigned), calling_window (array of structure) and destination_list (array of octet-string) as opaque encoded DLMS Data buffers prepared by the caller. Class version `2` by default (legacy v0 "PSTN auto dial" is still constructible by passing an explicit version), constructor normalization to `MaxSupportedVersion = 2`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). From class version `2` onward the IC defines one method `1 connect (data)`; the built-in object surfaces it as `UnsupportedFeature` (the backend is expected to drive the dialler/radio stack out-of-band). For instances pinned to legacy version `0` the method does not exist and `InvokeMethod` reports `MethodNotFound` for every id. |
| `30` | Data Protection | Partial | `CosemDataProtectionObject` exposes protection_buffer (octet-string carrying the protected payload), protection_object_list (array of structure {`protection_type`: enum, `protection_options`: structure, `protection_parameters_id`: octet-string}), protection_parameters_get (array of structure {`protection_type`: enum, `protection_options`: structure}), protection_parameters_set (array of structure {`protection_type`: enum, `protection_options`: structure}) and required_protection (bit-string with authentication / encryption / digital-signature bits) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed protection buffer, object list, parameter tables and required-protection mask after performing the protected operations out-of-band). Methods `1` `get_protected_attributes`, `2` `set_protected_attributes` and `3` `invoke_protected_method` return `UnsupportedFeature` (the built-in object does not perform protected operations); other method ids return `MethodNotFound`. |
| `40` | Push Setup | Partial | `CosemPushSetupObject` exposes push_object_list, send_destination_and_method, communication_window, randomisation_start_interval, number_of_retries and repetition_delay (v0 surface) plus port_reference, push_client_SAP and push_protection_parameters (added in v1) plus push_operation_method, confirmation_parameters and last_confirmation_date_time (added in v2) as opaque encoded DLMS Data buffers prepared by the caller, with `MaxSupportedVersion = 2` and constructor normalization. The mutable attributes (2-12 on v2; 2-10 on v1; 2-7 on v0) share a caller-selected `AttributeAccessMode`; logical_name and last_confirmation_date_time (13) are read-only (a setter exposes backend-driven refresh of attribute 13). V0 objects report `AttributeNotFound` for attributes 8-13 on both read and write, and v1 objects report `AttributeNotFound` for attributes 11-13. Note: the `repetition_delay` data type is long-unsigned in v0/v1 and a `{repetition_delay_min, repetition_delay_exponent, repetition_delay_max}` structure in v2; the buffer is opaque, the caller encodes the value that matches the negotiated version. Method `1` `push` is surfaced as `UnsupportedFeature` (application-defined push backend); method `2` `reset` (defined for v2 only) clears `last_confirmation_date_time` and returns `Ok` on v2 instances (and `MethodNotFound` on v0/v1); other method ids report `MethodNotFound`. |
| `41` | TCP-UDP Setup | Partial | `CosemTcpUdpSetupObject` exposes tcp_udp_port, ip_reference, mss, nb_of_sim_conn and inactivity_time_out as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `42` | IPv4 Setup | Partial | `CosemIpv4SetupObject` exposes DL_reference (octet-string), IP_address (double-long-unsigned), multicast_IP_address (array of double-long-unsigned), IP_options (array of structure), subnet_mask (double-long-unsigned), gateway_IP_address (double-long-unsigned), use_DHCP_flag (boolean), primary_DNS_address (double-long-unsigned) and secondary_DNS_address (double-long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`10` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). Methods `1` `add_mc_IP_address`, `2` `delete_mc_IP_address` and `3` `get_nbof_mc_IP_addresses` return `UnsupportedFeature` (built-in object does not own multicast subscription policy nor expose runtime multicast_IP_address array size); other method ids return `MethodNotFound`. |
| `43` | MAC Address Setup | Partial | `CosemMacAddressSetupObject` exposes mac_address (octet-string(6)) as an opaque encoded DLMS Data buffer prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; mac_address (`2`) honors a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `44` | PPP Setup | Partial | `CosemPppSetupObject` exposes PHY_reference (octet-string), LCP_options (array of structure), IPCP_options (array of structure) and PPP_authentication (structure of user_name/password) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`5` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `45` | GPRS Modem Setup | Partial | `CosemGprsModemSetupObject` exposes APN (octet-string), PIN code (long-unsigned) and quality_of_service (structure) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `46` | SMTP Setup | Partial | `CosemSmtpSetupObject` exposes server_port (long-unsigned, default 25), user_name (octet-string), login_password (octet-string), server_address (octet-string) and sender_address (octet-string) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `47` | GSM Diagnostic | Partial | `CosemGsmDiagnosticObject` exposes operator (octet-string), status (enum), circuit_switched_status (enum), packet_switched_status (enum), cell_info (structure), adjacent_cells (array of structure) and capture_time (date_time octet-string) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`8` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can publish refreshed diagnostic snapshots when read-write is granted). Per IEC 62056-6-2 ED4 (2021) §5.6.8 / DLMS UA Blue Book Ed. 12.1 §5.6.8, class_id `47`, version `0` defines no specific methods; every method id returns `MethodNotFound`. |
| `48` | IPv6 Setup | Partial | `CosemIpv6SetupObject` exposes data_link_layer_reference (octet-string LN), address_config_mode (enum), unicast_ip_address / multicast_ip_address (arrays of 16-byte octet-string), gateway_ip_address / primary_dns_address / secondary_dns_address (16-byte octet-string), traffic_class (unsigned) and neighbor_discovery_setup (array of structure) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`10` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed addressing after running the network stack out-of-band). Methods `1` `add_address` and `2` `remove_address` return `UnsupportedFeature` (the built-in object does not own network-stack semantics); other method ids return `MethodNotFound`. |
| `50` | S-FSK PLC PHY & MAC Setup | Partial | `CosemSFskPlcPhyMacSetupObject` exposes initiator/delta electrical phase, max receiving/transmitting gain, search_initiator_threshold, frequencies (structure { mark_frequency: double-long-unsigned, space_frequency: double-long-unsigned }), mac_address, mac_group_addresses, repeater, repeater_status, min_delta_credit, initiator_mac_address, synchronization_locked and transmission_speed as opaque encoded DLMS Data buffers prepared by the caller. Class version `1`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`15` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). Method `1` `reset` reports `UnsupportedFeature` and clears method output; undefined method ids report `MethodNotFound`. |
| `51` | S-FSK Active Initiator | Partial | `CosemSFskActiveInitiatorObject` exposes active_initiator (structure {system_title: octet-string(8), MAC_address: long-unsigned, L_SAP_selector: unsigned}, per IEC 62056-6-2 ED4 4.10.4.2.2 and DLMS UA Blue Book IC 51) as an opaque encoded DLMS Data buffer prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attribute `2` honours a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish a refreshed initiator descriptor after the S-FSK MAC tracks a new initiator out-of-band). Method `1` `reset_new_not_synchronized` reports `UnsupportedFeature` and clears method output; undefined method ids report `MethodNotFound`. |
| `52` | S-FSK MAC Synchronization Timeouts | Partial | `CosemSFskMacSyncTimeoutsObject` exposes search_initiator_timeout (long-unsigned, seconds), synchronization_confirmation_timeout (long-unsigned, seconds), time_out_not_addressed (long-unsigned, minutes) and time_out_frame_not_OK (long-unsigned, minutes) as opaque encoded DLMS Data buffers prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.10.5 and DLMS UA Blue Book Ed. 12.1 §4.10.5 (MIB variables from IEC 61334-4-512:2001 §5.3 and IEC 61334-5-1:2001 §4.3.7.6). Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`5` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the client can re-tune the MAC sub-layer timers after commissioning). IC defines no specific methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `53` | S-FSK MAC Counters | Partial | `CosemSFskMacCountersObject` exposes synchronization_register (array of structure {`mac_address`: long-unsigned, `synchronizations_counter`: double-long-unsigned}), desynchronization_listing (structure {`mac_address`: long-unsigned, `desynchronization_counter`: long-unsigned}), broadcast_frames_counter (array of structure {`mac_address`: long-unsigned, `frames_counter`: double-long-unsigned}), repetitions_counter (double-long-unsigned), transmissions_counter (double-long-unsigned), CRC_OK_frames_counter (double-long-unsigned) and CRC_NOK_frames_counter (double-long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.10.6 and DLMS UA Blue Book Ed. 12.1 §4.10.6 (MIB variables from IEC 61334-4-512:2001 §5.8). Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`8` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed counters and listings after the S-FSK MAC accumulates events out-of-band). Method `1` `reset` reports `UnsupportedFeature` and clears method output (counter bookkeeping is backend-owned); other method ids report `MethodNotFound`. |
| `55` | IEC 61334-4-32 LLC setup | Partial | `CosemIec61334432LlcSetupObject` exposes max_frame_length (long-unsigned, length of the LLC frame in bytes per IEC 61334-4-32:1996 §5.1.4; S-FSK profile min/def/max 26/134/242 per IEC 61334-5-1:2001 §4.2.2) and reply_status_list (array of structure {`L-SAP-selector`: unsigned, `length-of-waiting-L-SDU`: unsigned}; MIB variable reply-status-list (variable 11) per IEC 61334-4-512:2001 §5.4) as opaque encoded DLMS Data buffers prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.10.7 and DLMS UA Blue Book Ed. 12.1 §4.10.7. Class version `1` (ED4 deprecates `0`), constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`3` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed frame length and reply status list after the LLC sublayer accumulates events out-of-band). The class defines no specific methods; all method ids report `MethodNotFound`. |
| `56` | S-FSK Reporting system list | Partial | `CosemSFskReportingSystemListObject` exposes reporting_system_list (array of system-title where system-title ::= octet-string; MIB variable reporting-system-list (variable 16) per IEC 61334-4-512:2001 §5.7 — system-titles of server systems that issued a DiscoverReport CI_PDU and have not yet been registered, sorted by arrival with the newest first) as an opaque encoded DLMS Data buffer prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.10.8 and DLMS UA Blue Book Ed. 12.1 §4.10.8. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attribute `2` honors a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the CIASE backend can republish the list after DiscoverReport CI_PDUs arrive and registrations purge entries out-of-band). The class defines no specific methods; all method ids report `MethodNotFound`. |
| `57` | ISO/IEC 8802-2 LLC Type 1 setup | Partial | `CosemIso8802LlcType1SetupObject` exposes max_octets_ui_pdu (long-unsigned, default 128) as an opaque encoded DLMS Data buffer prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.11.2 and DLMS UA Blue Book Ed. 12.1 §4.11.2. Per ISO/IEC 8802-2:1998 §6.8.1 ("Maximum number of octets in a UI PDU") the LLC sublayer imposes no restriction, but for interoperability all MACs must accommodate UI PDUs with information fields up to and including 128 octets in length. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attribute `2` honors a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish the negotiated MAC ceiling after commissioning). The class defines no specific methods; all method ids report `MethodNotFound`. |
| `58` | ISO/IEC 8802-2 LLC Type 2 setup | Partial | `CosemIso8802LlcType2SetupObject` exposes the nine connection-oriented LLC Type 2 parameters (transmit_window_size_k, receive_window_size_rw, max_octets_i_pdu_n1, max_number_transmissions_n2, acknowledgement_timer, p_bit_timer, reject_timer, busy_state_timer) as opaque encoded DLMS Data buffers prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.11.3 and DLMS UA Blue Book Ed. 12.1 §4.11.3 (parameter semantics per ISO/IEC 8802-2:1998 §7.8.1–7.8.4). Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`9` share a caller-selected `AttributeAccessMode` so the backend can re-tune connection parameters after commissioning. The class defines no specific methods; all method ids report `MethodNotFound`. |
| `59` | ISO/IEC 8802-2 LLC Type 3 setup | Partial | `CosemIso8802LlcType3SetupObject` exposes the five acknowledged-connectionless LLC Type 3 parameters (max_octets_acn_pdu_n3, max_number_transmissions_n4, acknowledgement_time_t1, receive_lifetime_var_t2, transmit_lifetime_var_t3) as opaque encoded DLMS Data buffers prepared by the caller, per IEC 62056-6-2 ED4 (2021) §4.11.4 and DLMS UA Blue Book Ed. 12.1 §4.11.4. Parameter semantics per ISO/IEC 8802-2:1998 §8.6.1, §8.6.2 and the acknowledged-connectionless timer descriptions in the same clause set. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` so the backend can re-tune ACL parameters after commissioning. The class defines no specific methods; all method ids report `MethodNotFound`. |
| `61` | Register Table | Partial | `CosemRegisterTableObject` exposes `table_cell_values` and `table_cell_definition` as opaque encoded DLMS Data buffers prepared by the caller, and `scaler_unit` as the typed `dlms::cosem::types::ScalerUnit` (`structure { integer scaler, enum unit }` per §4.3.2.2.3). Class version `0`, constructor normalization to `MaxSupportedVersion`. `logical_name` and `table_cell_values` (`1`, `2`) are read-only; `table_cell_definition` and `scaler_unit` (`3`, `4`) share a caller-selected `AttributeAccessMode`. `ReadAttribute(4)` encodes the scaler-unit AXDR structure on every read; `WriteAttribute(4)` parses the AXDR structure and returns `InvalidArgument` for malformed payloads (preserving the stored value). A setter exposes backend-driven refresh of `table_cell_values`. Typed `table_cell_values` / `table_cell_definition` are pending the discriminated-union `value` infrastructure shared with IC 3/4/5. Spec-defined methods `1 reset` and `2 capture` drive the captured payload lifecycle and are surfaced as `UnsupportedFeature` (backend-owned); other method ids report `MethodNotFound`. |
| `62` | Compact Data | Partial | `CosemCompactDataObject` exposes typed `template_id` (`std::uint8_t`, AXDR `unsigned`) and typed `capture_method` (nested enum `CaptureMethod { Inactive=0, Invoke=1, InvokeAndStore=2 }`, AXDR `enum`), encoded/decoded on every read/write per IEC 62056-6-2 ED4 (2021) §4.3.10 / DLMS UA Blue Book Ed. 12.1 §4.3.10. `compact_buffer` (`2`), `capture_objects` (`3`) and `template_description` (`5`) remain opaque encoded DLMS Data buffers prepared by the caller, pending the typed discriminated-union machinery shared with IC 7 (Profile Generic). Class version `1`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode`. Constructor is safe-fallback: an out-of-range raw `capture_method` collapses to `Inactive` (the spec default). `WriteAttribute(template_id)` decodes AXDR `unsigned` (rejects wrong tag, truncation and trailing bytes with `InvalidArgument`); `WriteAttribute(capture_method)` decodes AXDR `enum`, validates the `0..2` range and rejects out-of-range / malformed payloads with `InvalidArgument` (stored value preserved on rejection). Method `1` `reset` is implemented in-place and clears `compact_buffer` only (configuration — `capture_objects`, `template_id`, `template_description`, `capture_method` — is preserved); method `2` `capture` returns `UnsupportedFeature` until the typed capture-object-definition machinery shared with IC 7 lands. Other method ids return `MethodNotFound`. |
| `63` | Status Mapping | Partial | `CosemStatusMappingObject` exposes status_word (bit-string carrying the raw status value) and mappings (array of structure {status_value: bit-string, mapped_value: bit-string}) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`3` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed status and mapping tables after evaluating the status word out-of-band). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `64` | Security Setup | Partial | `CosemSecuritySetupObject` defaults to class version `1`, allows explicit descriptor version selection up to `MaxSupportedVersion`, gates attribute `6` and methods `3`-`8` to version `1`, and exposes logical name, encoded security policy, security suite, client/server system titles. Attribute `6` `certificates` is encoded as a DLMS Data array of `certificate_info` structures from a pluggable `ICosemCertificateStore` (empty array when no store entries exist). `security_activate` enforces monotonic policy strengthening; suite `0` `global_key_transfer` is implemented through a mutable key store. Methods `6` (`import_certificate`), `7` (`export_certificate`) and `8` (`remove_certificate`) parse the Blue Book selector structures and dispatch to the certificate store backend, returning `UnsupportedFeature` when no store is attached; methods `3`/`4`/`5` (key agreement / generate key pair / generate certificate request) remain `UnsupportedFeature` until an ECDSA/X.509 stack is wired in. |
| `65` | Parameter Monitor | Supported | `CosemParameterMonitorObject` exposes `capture_time` (id 3) as typed `dlms::cosem::types::DateTime`, `parameter_list` (id 4) as typed `std::vector<dlms::cosem::types::MonitoredValue>` and `hash_algorithm_id` (id 6) as nested enum `HashAlgorithmId {Sha256=0, Sha384=1, Sha256Last16=2, Sha256Last8=3, Sha256Last4=4}`; `changed_parameter` (id 2), `parameter_list_name` (id 5), `parameter_value_digest` (id 7) and `parameter_values` (id 8) remain opaque encoded DLMS Data buffers pending the typed discriminated-union (CHOICE) machinery shared with IC 3/4/5/61. Class version `1` per IEC 62056-6-2 ED4 (2021) §4.5.10 (the constructor still accepts legacy version `0` from Blue Book 12.1 §5.4.1 with only attributes `1`-`4` exposed), with constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`8` share a caller-selected `AttributeAccessMode`. `WriteAttribute(3)`/`(4)`/`(6)` validate AXDR tag, length / structure and enum range and return `InvalidArgument` on malformed input (stored value preserved on rejection). The ctor normalises out-of-range raw `hash_algorithm_id` values to `Sha256`. Methods `1` `add_parameter` and `2` `delete_parameter` are implemented in-place per §4.5.10.2 (append / first-match remove on parameter_list), returning `InvalidArgument` for malformed input, `ObjectError` when no entry matches on delete, and `UnsupportedFeature` on legacy version-0 instances; other method ids return `MethodNotFound`. Reads/writes of attributes `5`-`8` on a legacy version-0 instance return `AttributeNotFound`. |
| `67` | Sensor Manager | Partial | `CosemSensorManagerObject` exposes the IEC 62056-6-2 ED4 (2021) §4.5.11 layout: serial_number (octet-string), metrological_identification (octet-string), output_type (enum), adjustment_method (octet-string), sealing_method (enum), raw_value (CHOICE), scaler_unit (structure), status (CHOICE), capture_time (date-time octet-string(12)), raw_value_thresholds / raw_value_actions (arrays), processed_value (processed_value definition) and processed_value_thresholds / processed_value_actions (arrays) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`15` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed sensor metadata, raw / processed values and thresholds after polling the slave out-of-band). Method `1` `reset(data)` returns `UnsupportedFeature` (the built-in object does not own the sensor lifecycle); other method ids return `MethodNotFound`. |
| `68` | Arbitrator | Partial | `CosemArbitratorObject` exposes actions (array of structure {script_logical_name, script_selector}), permissions_table (array of bit-string, one row per actor), weightings_table (array of array of long-unsigned), most_recent_requests_table (array of bit-string) and last_outcome (unsigned, index of the winning script) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed arbitration state after driving arbitration out-of-band). Methods `1` `request_action` and `2` `reset` return `UnsupportedFeature` (the built-in object does not own arbitration semantics); other method ids return `MethodNotFound`. |
| `70` | Disconnect Control | Supported | `CosemDisconnectControlObject` exposes typed values for all three dynamic attributes per IEC 62056-6-2 ED4 §4.5.8 / DLMS UA Blue Book Ed. 12.1 §4.5.8: `output_state` is a `bool` (read-only via `WriteAttribute`, refreshed by the backend through `SetOutputState`), `control_state` is the nested `ControlState` enum `{Disconnected=0, Connected=1, ReadyForReconnection=2}` (read-only, refreshed through `SetControlState`), and `control_mode` is the nested `ControlMode` enum `{Mode0..Mode6}` honoring a caller-selected `AttributeAccessMode`. `ReadAttribute` encodes the typed values as AXDR (`boolean`, `enum`) on every read; `WriteAttribute(4)` parses the AXDR enum, validates the range `0..6` and returns `InvalidArgument` for malformed payloads (the stored value is preserved on rejection). Methods `1` `remote_disconnect` and `2` `remote_reconnect` implement the state-machine transitions of §4.5.8.3 directly inside the built-in object: `remote_disconnect` returns `UnsupportedFeature` in `Mode0`, otherwise sets `control_state = Disconnected` and `output_state = false`; `remote_reconnect` returns `UnsupportedFeature` in `Mode0`, transitions to `Connected` (output `true`) in modes 2/4 and to `ReadyForReconnection` (output `false`) in modes 1/3/5/6. The `data` argument is tolerated (real meters often invoke with empty input). Other method ids return `MethodNotFound`. Class version `0`, constructor normalization to `MaxSupportedVersion`. |
| `71` | Limiter | Partial | `CosemLimiterObject` exposes monitored_value, threshold_active, threshold_normal, threshold_emergency, min_over_threshold_duration, min_under_threshold_duration, emergency_profile, emergency_profile_group_id_list, emergency_profile_active and actions as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name and monitored_value (`1`, `2`) are read-only; threshold/duration/emergency/actions attributes (`3`-`11`) share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). Setters expose backend-driven refresh of threshold_active and emergency_profile_active from a future limiter backend. IC v0 defines no methods; `InvokeMethod` reports `MethodNotFound` for all ids. |
| `72` | M-Bus Client | Partial | `CosemMBusClientObject` exposes mbus_port_reference (octet-string(6) LN to IEC HDLC Setup), capture_definition (array of structure {`data_link_reference`: octet-string, `value_information_block`: octet-string}), capture_period (double-long-unsigned, seconds), primary_address (unsigned), identification_number (double-long-unsigned), manufacturer_id (long-unsigned), version (unsigned, M-Bus device version), device_type (unsigned), access_number (unsigned), status (unsigned), alarm (unsigned), configuration (long-unsigned, v1) and encryption_key_status (enum, v1) as opaque encoded DLMS Data buffers prepared by the caller. Class version `1` (constructor normalization to `MaxSupportedVersion`); attributes `13` (configuration) and `14` (encryption_key_status) are exposed only when the requested version is `>=1` per IEC 62056-6-2:2021 4.8.3, while v0 (Blue Book 12.1 5.7.1) reports `NoAccess` and returns `AttributeNotFound` for them. logical_name (`1`) is read-only; attributes `2`-`12` (plus `13`-`14` on v1) share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed identification, status, alarm, configuration and key-status payloads after driving the M-Bus slave out-of-band). Methods `1` `slave_install`, `2` `slave_deinstall`, `3` `capture`, `4` `reset_alarm`, `5` `synchronise_clock`, `6` `send_data`, `7` `set_encryption_key` and `8` `transfer_key` return `UnsupportedFeature` (the built-in object does not drive the M-Bus slave); other method ids return `MethodNotFound`. |
| `73` | Wireless Mode Q channel | Partial | `CosemWirelessModeQChannelObject` exposes `addr_state` (enum: 0 not assigned / 1 assigned), `device_address` (octet-string carrying the currently assigned network address) and `address_mask` (octet-string carrying the group address responded to under short-form addressing) as opaque encoded DLMS Data buffers prepared by the caller per IEC 62056-6-2 ED4 (2021) §4.8.4 and DLMS UA Blue Book Ed. 12.1 §4.8.3 (see also EN 13757-5:2015). Class version `1`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed address state, device address and address mask after over-the-air commissioning or re-keying performed out-of-band). IC defines no specific methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `74` | M-Bus Master Port Setup | Partial | `CosemMBusMasterPortSetupObject` exposes comm_speed (enum) as an opaque encoded DLMS Data buffer prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; comm_speed (`2`) honors a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `75`-`76` | M-Bus classes | Application-provided | No built-in implementation. |
| `77` | M-Bus Diagnostic | Partial | `CosemMBusDiagnosticObject` exposes received_signal_strength (unsigned, dBm), channel_id (unsigned), link_status (enum), broadcast_frames_counter (array), transmissions_counter (double-long-unsigned), fcs_ok_frames_counter (double-long-unsigned), fcs_nok_frames_counter (double-long-unsigned) and capture_time (date-time octet-string(12)) as opaque encoded DLMS Data buffers prepared by the caller per IEC 62056-6-2 ED4 (2021) §4.8.7. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`9` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the backend can republish refreshed signal strength, channel id, link status and counters after polling the M-Bus link out-of-band). Method `1` `reset` (optional) returns `UnsupportedFeature` because the built-in object does not own the counter sources; undefined method ids return `MethodNotFound`. |
| `80` | 61334-4-32 LLC SSCS Setup | Partial | `CosemPrimePlcLlcSscsSetupObject` exposes `service_node_address` and `base_node_address` (long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller per IEC 62056-6-2 ED4 (2021) §4.12.3 / DLMS UA Blue Book Ed. 12.1 §4.12.3. Holds the addresses provided by the base node during opening of the convergence layer; after deregistration the spec sets `service_node_address = NEW = 0x0FFE` and `base_node_address = 0x0000`. Class version `0`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`3` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the PRIME convergence-layer backend can republish refreshed addresses as the base/service node registers or deregisters). Spec defines one specific method `reset(data)` (data ::= integer(0), method id `1`) for deallocating the service node address; actual address deallocation is owned by the PRIME convergence-layer backend, so `InvokeMethod` reports `UnsupportedFeature` for method id `1` and `MethodNotFound` for every other method id. |
| `82` | PRIME PLC MAC Setup | Partial | `CosemPrimePlcMacSetupObject` exposes mac_min_con_window, mac_max_con_window (long-unsigned), mac_channel_access_fairness_limit (unsigned), mac_EMA, mac_SAR_size, mac_max_PDU_size, mac_min_switch_search_time, mac_max_promotion_PDU, mac_promotion_PDU_TX_period (long-unsigned), mac_beacons_per_frame, mac_scp_max_TX_attempts, mac_CTL_re_TX_timer (unsigned) and mac_max_LNID (long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller per IEC 62056-6-2 ED4 (2021) §4.12.6. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`14` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `81` | PRIME PLC Physical Layer Counters | Partial | `CosemPrimePlcPhyLayerCountersObject` exposes `phy_stats_crc_incorrect_count` (PIB 0x00A0), `phy_stats_crc_failed_count` (PIB 0x00A1), `phy_stats_tx_drop_count` (PIB 0x00A2) and `phy_stats_rx_drop_count` (PIB 0x00A3) as opaque encoded DLMS Data buffers (long-unsigned) prepared by the caller per IEC 62056-6-2 ED4 (2021) §4.12.5 and DLMS UA Blue Book Ed. 12.1 §4.12.4. Class version `0`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`5` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the PLC stack backend can republish refreshed PHY statistics buffers as the PRIME node updates its PIB counters; counters are read-only per spec but the access-mode hook is what lets the framework receive those updates). Spec defines a single specific method `reset(data)` (data ::= integer(0), method id `1`) for clearing the counters; actual zeroing is owned by the PLC backend, so `InvokeMethod` reports `UnsupportedFeature` for method id `1` and `MethodNotFound` for every other method id. |
| `83` | PRIME PLC MAC Functional Parameters | Yes | `CosemPrimePlcMacFunctionalParametersObject` exposes the full 14-attribute v0 spec: lnid (long-unsigned), lsid (unsigned), sid (unsigned), sna (octet-string), state (enum), scp_length (long), node_hierarchy_level (unsigned, 0..63), beacon_slot_count (unsigned, 0..7), beacon_rx_slot (unsigned, 0..7), beacon_tx_slot (unsigned, 0..7), beacon_rx_frequency (unsigned, 0..31), beacon_tx_frequency (unsigned, 0..31) and capabilities (long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`14` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). IC defines no methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |
| `84` | PRIME PLC MAC Counters | Partial | `CosemPrimePlcMacCountersObject` exposes txdatapkt_count, rxdatapkt_count, txctrlpkt_count, rxctrlpkt_count, csmafail_count and csmachbusy_count (double-long-unsigned) as opaque encoded DLMS Data buffers prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. logical_name (`1`) is read-only; attributes `2`-`7` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted). Method `1` `reset` reports `UnsupportedFeature` and clears method output; undefined method ids report `MethodNotFound`. |
| `85` | PRIME PLC MAC Network Administration Data | Partial | `CosemPrimePlcMacNetworkAdminDataObject` exposes the five dynamic array attributes defined by IEC 62056-6-2 ED4 (2021) §4.12.9 / DLMS UA Blue Book Ed. 12.1 §4.12.9: `mac_list_multicast_entries` (PIB attribute `0x0052`), `mac_list_switch_table` (PIB attribute `0x0053`), `mac_list_direct_table` (PIB attribute `0x0055`), `mac_list_available_switches` (PIB attribute `0x0056`) and `mac_list_phy_comm` (PIB attribute `0x0057`). Each attribute is an opaque encoded DLMS Data array buffer prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`6` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the management backend can republish refreshed PRIME MAC network state). Specific method `1 reset(data)` (optional per spec) reports `UnsupportedFeature` because the backend owns the table snapshots; other method ids report `MethodNotFound`. Realigned in `0.108.0` from an earlier non-spec layout that exposed four invented counters (`node_registrations`, `node_unregistrations`, `processed_alive_msgs`, `handled_promotions`). |
| `84` | _(reserved — see `43`)_ | Application-provided | IEC 62056-6-2 ED4 (2021) §4.12.10 places the PRIME NB OFDM PLC MAC address setup IC at class_id `43`, not `84`. Use the built-in `CosemMacAddressSetupObject` (row `43`). The previous `CosemPrimePlcMacAddressSetupObject` (class_id `84`) was a duplicate and was removed in 0.91.0. |
| `86` | PRIME PLC Application Identification | Partial | `CosemPrimePlcApplicationIdentificationObject` exposes the three identification attributes defined by IEC 62056-6-2 ED4 (2021) §4.12.11 / DLMS UA Blue Book Ed. 12.1 §4.12.11: `firmware_version` (octet-string, max 128, PIB attribute `0x0075`), `vendor_Id` (long-unsigned, PIB attribute `0x0076`) and `product_Id` (long-unsigned, PIB attribute `0x0077`). Each attribute is an opaque encoded DLMS Data buffer prepared by the caller. Class version `0`, constructor normalization to `MaxSupportedVersion`. `logical_name` (`1`) is read-only; attributes `2`-`4` share a caller-selected `AttributeAccessMode` (writes replace the stored buffer in-place when permitted, so the management backend can republish refreshed identifiers as the firmware updates). IC defines no specific methods; `InvokeMethod` reports `MethodNotFound` for all method ids and clears method output. |

## СПОДЭС/СПОДУС Application Model

| Area | Status | Notes |
| --- | --- | --- |
| Meter categories A/B/C/D parameter lists | Planned | Need catalog definitions and conformance tests. |
| СПОДУС ИВКЭ object model | Planned | Needs separate object catalog for ИВКЭ. |
| Event code table | Planned | Required by СПОДЭС/СПОДУС event journals. |
| Status word formats | Planned | Required by СПОДЭС status model. |
| Profile `0.0.94.7.131.255` discovered meters | Planned | Profile Generic with fixed column schema. |
| Data `0.0.94.7.132.255` meter access policies | Planned | Includes security policy/key list modeling. |
| Profile `0.0.94.7.135.255` exchange status journal | Planned | Profile Generic with task status columns. |
| Profile `0.0.94.7.136.255` object correction journal | Planned | Profile Generic with correction timestamps. |
| Profile `0.0.94.7.137.255` meter parameter journal | Planned | Profile Generic with one parameter value per row. |
| Profile `0.0.94.7.140.255` event aggregation | Planned | Profile Generic with event metadata and transfer status. |
| Push notification payload model | Planned | Needed to send only actual data not yet transferred/confirmed where required. |

## Production Gate

The COSEM layer is not complete for СПОДЭС/СПОДУС production use until:

1. The IC matrix is covered by automated tests for every claimed `Supported`
   class.
2. СПОДЭС/СПОДУС OBIS catalogs and category-specific parameter lists are
   represented as data, not informal documentation.
3. Profile Generic selective access, capture objects and fixed journal schemas
   are implemented.
4. Association LN object list and access rights are generated for the active
   association/security context.
5. Unsupported ICs return explicit access/service errors through server and
   endpoint paths.

The `0.4.11` knowledge-base audit reconciled this matrix against ГОСТ Р
58940-2020 table 7.1 and the DLMS UA Blue Book class list. Built-in coverage
was limited to Data `1`, Register `3`, Association LN `15`, SAP Assignment
`17`, and partial Security Setup `64`; `0.5.0` adds partial Profile Generic
`7` scaffolding.
